name: Paper scout moved

on:
  workflow_dispatch:

permissions: {}

jobs:
  moved:
    runs-on: ubuntu-latest
    steps:
      - name: Show the active repository
        run: echo "Paper scouting has moved to meteroad/intelligent-audio-production-resources."
